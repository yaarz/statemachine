public class EventTwo extends Event {
    public String eventTwoData; // added to show that we can have a difference between event one and two, not used
    public EventTwo (String event,int id){
        super(event,id);
    }
}
