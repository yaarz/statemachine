public class EventOne extends Event {
    public String eventOneData; // added to show that we can have a difference between event one and two, not used
    public EventOne(String event,int id){
       super(event,id);
    }
}
